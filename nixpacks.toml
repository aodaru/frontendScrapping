[phases.setup]
nixPkgs = ['nodejs_22']
aptPkgs = ['curl', 'wget']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm build']

[start]
cmd = 'pnpm preview'

[variables]
NODE_ENV = 'production'
