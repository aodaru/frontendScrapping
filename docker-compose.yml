services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - ix-worklist_default
networks:
  ix-worklist_default:
    external: true
